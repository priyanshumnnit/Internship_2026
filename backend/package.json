{
  "name": "blue-collar-backend",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "dev": "nodemon src/index.js",
    "start": "node src/index.js",
    "prisma": "prisma generate",
    "prisma:generate": "prisma generate",
    "prisma:db:push": "prisma db push",
    "prisma:migrate:status": "node -e \"console.log('Prisma Migrate is not supported for MongoDB. Use `npm run prisma:db:push` instead.')\"",
    "prisma:migrate:deploy": "node -e \"console.log('Prisma Migrate is not supported for MongoDB. Use `npm run prisma:db:push` instead.')\"",
    "seed": "node prisma/seed.js",
    "migrate:sql-to-mongo": "node scripts/migrate-postgres-to-mongo.js"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "bcryptjs": "^2.4.3",
    "cloudinary": "^1.37.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "multer": "^2.1.1",
    "nodemailer": "^6.9.4",
    "pg": "^8.20.0",
    "prisma": "^5.14.0",
    "razorpay": "^2.9.6",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
